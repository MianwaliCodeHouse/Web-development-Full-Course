// variables must be declare using these keywords(variable declaration , use and scope)

// ?var variable_name (var used when your application will run on old browser,It can be redeclare and used before declaration,it has a globel scope)

// --------------

// ?let variable_name (let mostly used Nowadays because it can not redeclare and can not used before declaration , it has a block scope)

// ---------------

// ?const variable_name(const mostly used for making static variable , it can not redeclare and reassigned,it has a block scope)


